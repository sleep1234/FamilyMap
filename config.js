const path = require('path');

module.exports = {
  PORT: parseInt(process.env.PORT) || 8090,
  HOST: process.env.HOST || '0.0.0.0',
  DB_PATH: process.env.DB_PATH || path.join(__dirname, 'familymap.db'),
  AMAP_KEY: process.env.AMAP_KEY || '',
  AMAP_JS_KEY: process.env.AMAP_JS_KEY || '',
  SESSION_EXPIRE_DAYS: 7,
  CACHE_FRESH_DAYS: 90,
  CACHE_STALE_DAYS: 180,
  GEOCODE_THROTTLE_MS: 30000,
  GEOCODE_REFRESH_DAY: 1,
  GEOCODE_REFRESH_HOUR: 3,
  GEOCODE_REFRESH_LIMIT: 1000,
  STAY_DRIFT_TOLERANCE: 200,
  STAY_SPEED_THRESHOLD: 3.0,
  STAY_LEAVE_CONFIRM_COUNT: 2,
  STAY_NOTIFY_MIN_DURATION: 10, // 停留不足10分钟离开时不发送通知（避免路口等红灯误报）
  STAY_TIMEOUT_HOURS: 24,
  GPS_ACCURACY_THRESHOLD: 50,
  COLLISION_HIGH_SPEED: 60,
  COLLISION_HARD_BRAKE_THRESHOLD: 20,
  COLLISION_COOLDOWN_MS: 180000,
  LOW_BATTERY_THRESHOLD: 15,
  LOW_BATTERY_INTERVAL_MS: 3600000,
  ALIVE_WARNING_HOURS: 24,
  SHARE_DEFAULT_DURATION_MINUTES: 60,
  SHARE_TOKEN_BYTES: 32,
  SHARE_MAX_DURATION_MINUTES: 1440,
  RATE_LIMIT_WINDOW_MS: 60000,
  RATE_LIMIT_MAX_REQUESTS: 100,
  RATE_LIMIT_LOGIN_MAX: 5,
  BCRYPT_ROUNDS: 12,
  DETECTION_THROTTLE_MS: 2000,
  STAY_THROTTLE_MS: 5000,
  GEOFENCE_THROTTLE_MS: 2000,
  MEMORY_MAP_MAX_SIZE: 10000,
  SOCKET_EVENT_THROTTLE_MS: 200,
  LOCATION_RATE_LIMIT_MAX: 30,
};
